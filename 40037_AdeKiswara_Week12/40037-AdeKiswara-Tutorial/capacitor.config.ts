import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '40037-AdeKiswara-Tutorial',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
